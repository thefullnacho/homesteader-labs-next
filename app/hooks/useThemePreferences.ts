"use client";

// Theme is permanently dark — no toggle. Hook kept as a stub for any legacy imports.
export function useThemePreferences() {
  return { isDark: true, lowFX: false };
}

export default useThemePreferences;
